# group: smoke
# stage: general
# status: active
# owner: thesis_multitask
# tags: [smd, w100, seed7, smoke, val_realistic]

experiment_name: smd_smoke_checkpoint_10ep_tiny
seed: 7
device: cpu
output_dir: outputs/smd_smoke_checkpoint_10ep_tiny
checkpoint_dir: outputs/smd_smoke_checkpoint_10ep_tiny/checkpoints
data_config_path: configs/data/smd_smoke_tiny_checkpoint_test.yaml
model_config_path: configs/model/reconstruction_mlp_ae.yaml
task_config_path: configs/task/reconstruction.yaml
optimizer:
  learning_rate: 0.001
  weight_decay: 0.0
  gradient_clip_norm: 0.5
epochs: 10
logging:
  use_wandb: false
