dataset_name: smd
root_dir: data/ServerMachineDataset
entity_ids:
  - machine-1-6
window_size: 20

stride: 1 # split nào không định nghĩa stride thì dùng stride này.
train_stride: 1
val_stride: 20
test_stride: 20

batch_size: 512
num_workers: 12
validation_split_ratio: 0.2
shuffle_train: false
