dataset_name: smd
root_dir: data/ServerMachineDataset
entity_ids:
  - machine-2-1
window_size: 100
# Sử dụng các cửa sổ không chồng lấp
# non-overlapping windows
stride: 100
batch_size: 512
num_workers: 8
validation_split_ratio: 0.2
shuffle_train: true
