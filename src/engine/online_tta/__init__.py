from __future__ import annotations

"""Online TTA helper package.

This package intentionally avoids eager re-exports so importing one helper
module does not pull the full online runtime graph into memory.
"""
